//@version=6
// NOTE: 原始因子使用信噪比归一化权重，此处用日线信号噪声比近似。
strategy("SNR_ENHANCED_REVERSAL - SNR-Weighted Reversal", overlay=false,
     fill_orders_on_standard_ohlc=true,
     default_qty_type=strategy.percent_of_equity, default_qty_value=100)

lookback = input.int(20, "Lookback")
rank_len = input.int(120, "Rank Length")
long_thresh = input.float(0.8, "Long Threshold")
short_thresh = input.float(0.2, "Short Threshold")

signal = math.abs(close - close[20])
noise = ta.sma(high - low, lookback)
snr = signal / noise
reversal = -(close / close[20] - 1)
factor = snr * reversal

rank = ta.percentrank(factor, rank_len) / 100.0

if barstate.isconfirmed
    if rank > long_thresh
        strategy.entry("Long", strategy.long)
    else if rank < short_thresh
        strategy.entry("Short", strategy.short)
    else
        strategy.close_all()

plot(factor, "SNRReversal", color.new(color.yellow, 0))
plot(rank, "Rank", color.new(color.blue, 0))
