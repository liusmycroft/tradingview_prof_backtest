//@version=6
// NOTE: 原始因子使用量涌分段收益率标准差，此处用日线高成交量日收益率标准差近似。
strategy("VOLUME_SURGE_VOL - Volume Surge Volatility", overlay=false,
     fill_orders_on_standard_ohlc=true,
     default_qty_type=strategy.percent_of_equity, default_qty_value=100)

lookback = input.int(20, "Lookback")
rank_len = input.int(120, "Rank Length")
long_thresh = input.float(0.2, "Long Threshold")
short_thresh = input.float(0.8, "Short Threshold")

surge = volume > 1.5 * ta.sma(volume, lookback)
surge_ret = surge ? (close / close[1] - 1) : 0.0
factor = ta.stdev(surge_ret, lookback)

rank = ta.percentrank(factor, rank_len) / 100.0

if barstate.isconfirmed
    if rank < long_thresh
        strategy.entry("Long", strategy.long)
    else if rank > short_thresh
        strategy.entry("Short", strategy.short)
    else
        strategy.close_all()

plot(factor, "Volume Surge Vol", color.new(color.yellow, 0))
plot(rank, "Rank", color.new(color.blue, 0))
