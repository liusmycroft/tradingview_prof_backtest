//@version=6
// NOTE: 原始因子使用日度价量相关系数序列回归斜率，此处用差分近似。
strategy("PV Corr Trend (PV_CORR_TREND)", overlay=true, default_qty_type=strategy.percent_of_equity, default_qty_value=100, fill_orders_on_standard_ohlc=true)
i_T = input.int(20, "Correlation Window", minval=5)
pv_corr = ta.correlation(close, volume, i_T)
factor = (pv_corr - pv_corr[i_T]) / i_T
factor_rank = ta.percentrank(factor, 120) / 100.0
if barstate.isconfirmed
    if factor_rank > 0.8 and strategy.position_size >= 0
        strategy.entry("Short", strategy.short)
    else if factor_rank < 0.2 and strategy.position_size <= 0
        strategy.entry("Long", strategy.long)
plot(factor, "Factor", color=color.new(color.orange, 0), display=display.pane)
